# 2026-04-21

- Implemented TaskMaster PDF uploads and preview: local `uploads/` storage, `/api/upload`, `/api/uploads/[filename]`, `react-pdf` viewer, task form attachment support, `pdfPath` on tasks, card PDF indicator, and task detail dialog preview.
- `react-pdf` needed a same-origin `/api/pdf-worker` route and client-only dynamic loading to avoid Next production build failures from server-side `DOMMatrix` and pdf.js worker bundling.
- Implemented TaskMaster subtasks: `/api/subtasks` create route, `/api/subtasks/[id]` patch/delete routes, typed subtask DTOs, task responses including ordered subtasks, inline subtask add/check/delete UI in task detail, and task-card subtask progress bars calculated from completed subtasks.
- Implemented TaskMaster UI polish: task status filter tabs, title search, empty states, Sonner toasts, Radix/shadcn-style delete confirmation dialog, per-action loading indicators, richer status badges, and transition/hover styling. Added `sonner` and `@radix-ui/react-alert-dialog`; `npm run build` passes.
- Prepared TaskMaster for deployment: switched to standalone Next config, added `.env.example`, production README, Dockerfile with migrator stage, `.dockerignore`, `/api/health` database check, Prisma install/migration scripts, and ignored `.env`, `.data`, and upload contents. `npm run build`, `npm run lint`, and standalone `/api/health` smoke test pass.

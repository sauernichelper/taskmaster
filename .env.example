# Required
# SQLite is the default local database. For production, prefer a persistent
# volume or a managed database; local SQLite files on serverless platforms are
# ephemeral.
DATABASE_URL="file:./dev.db"

# Optional
# Set to 1 in CI or container builds if you want to disable Next.js telemetry.
NEXT_TELEMETRY_DISABLED=1
